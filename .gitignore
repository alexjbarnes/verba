# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test model files (large, downloaded separately)
test_models/

# Native build caches (sherpa-onnx libs, ORT dylib)
.android-deps/
.desktop-deps/

# Signed APK artifacts
*.apk
*.idsig

# Debug signing key (generated by `just apk`)
debug.keystore

# Core dumps
core
src-tauri/data/grammar/*
!src-tauri/data/grammar/cross_attn_kv_weights.*.bin
!src-tauri/data/grammar/config.*.json

# Local test audio files
*.mp3
*.wav
*.m4a

# Stray icon drafts at repo root (real icons live in src-tauri/icons/)
/icon*.png

# Temp files from ONNX shape inference
*.data
sym_shape_infer_temp.onnx

# Playwright MCP
.playwright-mcp/

# Secrets / local env (R2 upload credentials etc.)
.env
.env.*

# Local agent tooling (machine-specific, never committed)
.graphene/
__pycache__/

# Claude Code: skills are checked in (shared project knowledge); transient
# per-session state (worktree scratch) stays local.
.claude/worktrees/

# Local R2 upload staging (models live on R2, not in git)
r2-staging/
